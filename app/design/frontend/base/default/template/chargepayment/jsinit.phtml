
<script type="text/javascript">


    $buttonCheckout =  null;
    if($buttonCheckout = $$('#checkout-review-submit .btn-checkout')) {
        var eventClick = $buttonCheckout[$buttonCheckout.length-1].readAttribute('onclick');
        <?php if($this->isSelected()): ?>

        if(typeof Checkout !='undefined' && Checkout.hasOwnProperty('render')) {
            $buttonCheckout.each(function(item,index){
                $buttonCheckout[index].writeAttribute('onclick','Checkout.open()');
            });

        }else   if(typeof CheckoutIntegration !='undefined' && CheckoutIntegration.hasOwnProperty('render')) {

            $buttonCheckout.each(function(item,index){
                $buttonCheckout[index].writeAttribute('onclick','CheckoutIntegration.open()');
            });
        }

        <?php endif; ?>
        $buttonCheckout.each(function(item,index){
            $buttonCheckout[index].writeAttribute('data-clickevent',eventClick);
        });



    }


</script>
